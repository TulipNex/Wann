# File: .env.example (Format: ENV / Environment Variable)
# Konfigurasi Bot
# Gunakan koma untuk memisahkan prefix (Multi Prefix)
PREFIX=!,.,/,#
OWNER_NUMBER=6282215415550,249348621369556@lid

Sistem Hot Reload ini sekarang mengawasi seluruh struktur bot Anda. Perubahan pada plugin di `commands/` akan terasa instan, sementara perubahan pada sistem inti akan memicu *auto-reboot* yang ditangani oleh panel Pterodactyl.

# Konfigurasi Database (Ganti dengan URL MongoDB Anda)
MONGODB_URI=mongodb+srv://Mitra:280403@cluster0.2zs4ym8.mongodb.net/?appName=Cluster0

# Konfigurasi Environment (dev / prod)
NODE_ENV=development